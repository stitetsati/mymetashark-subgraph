{
  "name": "mymetashark-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "format": "./tools/format",
    "checkFormat": "./tools/checkFormat",
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ mymetashark-subgraph",
    "create-local": "graph create --node http://localhost:8020/ mymetashark-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ mymetashark-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 mymetashark-subgraph",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.49.0",
    "@graphprotocol/graph-ts": "0.29.1",
    "prettier": "^2.6.2"
  },
  "devDependencies": { "matchstick-as": "0.5.0" },
  "prettier": {
    "printWidth": 200
  }
}
